# cos-mcp-server

North Star OS Chief of Staff MCP server — source of truth repo.

Seed commit to initialize the repository. Real source (netlify.toml, package.json, netlify/functions/health.js, netlify/functions/mcp.js) follows via Claude push immediately after this.
